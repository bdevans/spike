xgrid -job submit -in ~/Code/Spike/exec/ -out ~/Code/Spike/exec/Results Spike -rdf fast.prm

xgrid -job results -id 912427 -out ~/Code/Spike/exec/Results/ -so ./Spike.out

% Xgrid: xgrid -h mac0.cns.ox.ac.uk -auth Kerberos -job list
% export XGRID_CONTROLLER_HOSTNAME=mac0.cns.ox.ac.uk

% xgrid -auth Kerberos -job results -id <id>
% xgrid -auth Kerberos -job attributes -id <id>
% xgrid -auth Kerberos -job submit <script|executable>

% Specifying the full path prevents copying the executable to agents
% -in ~/Code/Spike % Specify working directory
% -email benjamin.evans@psy.ox.ac.uk % Notify about status change

% $ xgrid -job submit -in ~/data/working myscript param1 param2
% { jobIdentifier = 27; }
% $ xgrid -job results -id 27 -so job.out -se job.err -out job-outdir
% $ xgrid -job delete -id 27
